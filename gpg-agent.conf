# Restart the agent - `gpgconf --kill gpg-agent`
# Requires `echo "export GPG_TTY=$(tty)" >> ~/.zprofile`

allow-emacs-pinentry
allow-loopback-pinentry
default-cache-ttl 28800 # Remember passphrase for 8 hours
max-cache-ttl 28800
